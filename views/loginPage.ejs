<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Login Page</title>
    <link rel="stylesheet" href="styles/loginPage.css" type="text/css">
  </head>
  <body>
    <h1>LOGIN PAGE</h1>

    <% if (msg === 'success') { %>
        <p>Parabéns!! Você se registrou com sucesso, agora é só você logar e utilizar o sistema.</p>
      <% } %> 

      <% if (msg === 'registrado') { %>
        <p>Você já está registrado! Basta logar.</p>
     <% } %>

     <% if (msg === 'naoencontrado') { %>
      <p>Me parece que você ainda não está registrado</p>
    <% } %>

    <% if (msg === 'invalido') { %>
      <p>Algum do seus dados está inválido. Tente novamente.</p>
   <% } %>

    <form action="/loginPage" method="POST">
        <label for="email">E-mail:</label>
        <input type="email" id="email" name="email" required>
        <br>
        <label for="password">Senha:</label>
        <input type="password" id="password" name="password" required>
        <br>
        <button type="submit">Entrar</button>
      </form>  
      
      <a href="/register">BOTÃO DE REGISTRO</a>

</body>
</html>
